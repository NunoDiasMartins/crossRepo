import React from 'react';
import ReactDOM from 'react-dom/client';
import LegacyChatbotApp from './LegacyChatbotApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <LegacyChatbotApp />
  </React.StrictMode>
);
