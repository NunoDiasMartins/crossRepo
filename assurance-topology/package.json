{
  "name": "assurance-topology",
  "private": true,
  "version": "0.1.0",
  "description": "Framework-agnostic Telco Assurance Topology UI monorepo",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm --workspace packages/demo run dev",
    "lint": "npm run -ws lint"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
